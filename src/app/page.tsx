import MainBanner from "@/components/MainBanner";
import React, { FC } from "react";

const Home: FC = () => {
	return (
		<main className="page-container">
			<MainBanner />
		</main>
	);
};

export default Home;
